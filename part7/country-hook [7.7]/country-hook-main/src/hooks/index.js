//they already add useCountry
/*import { useState } from 'react'

export const useCountry = (name) => {
  const [value, setValue] = useState('')

  const onChange = (event) => {
    //console.log('value', value)
    setValue(event.target.value)
  }

  return {
    name,
    value,
    onChange
  }
}*/